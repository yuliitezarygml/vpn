"protocol": "vless",
"settings": {
  "vnext": [{
    "address": "{{ ctx.proxy.server }}",
    "port": {{ ctx.proxy.port }},
    "users": [{
      "id": "{{ ctx.user.uuid }}",
      "encryption": "none",
      "flow": "{% if ctx.proxy.is_reality and ctx.proxy.transport == 'tcp' %}{{ ctx.hconfig.vless_flow or 'xtls-rprx-vision' }}{% else %}{{ ctx.hconfig.vless_flow }}{% endif %}",
      "level": 8
    }]
  }]
}
