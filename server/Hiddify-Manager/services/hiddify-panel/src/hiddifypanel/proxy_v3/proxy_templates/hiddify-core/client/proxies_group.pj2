{% if client_proxy_tags %}
{
  "type": "selector",
  "tag": "Select",
  "outbounds": [
    "Auto",
    {% for tag in client_proxy_tags %}
    "{{ tag }}",
    {% endfor %}
  ],
  "default": "Auto"
},
{
  "type": "urltest",
  "tag": "Auto",
  "outbounds": [
    {% for tag in client_proxy_tags %}
    "{{ tag }}",
    {% endfor %}
  ],
  "url": "https://www.gstatic.com/generate_204",
  "interval": "10m",
  "tolerance": 200
},
{% endif %}
