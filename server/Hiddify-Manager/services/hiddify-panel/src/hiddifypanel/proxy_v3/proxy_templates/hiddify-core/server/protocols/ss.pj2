"type": "shadowsocks",
"method": "{{ ctx.hconfig.shadowsocks2022_method }}",
"password": "{{ ctx.hconfig.shared_secret.replace('-','')|b64encode }}",
"users": [
  {% for user in ctx.users %}
  {
    "name": "{{ user.uuid }}", 
    "password": "{{ user.uuid_hex|b64encode }}"
  },
  {% endfor %}
]
