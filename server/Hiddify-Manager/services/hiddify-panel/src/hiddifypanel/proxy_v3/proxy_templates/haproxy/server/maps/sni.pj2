############## sub_link_only ##############
{%- for d in ctx.domains if d.mode == 'sub_link_only' %}
{{ d['domain'] |ljust(30)}} to_https_in_http_mode
{% endfor %}
############## end of sub_link_only ##############

############## sni gateway ##############
{%- for p in ctx.proxies if p.mode == "domains_sni_gateway" %}
# {{ p.slug }}
{%- for pd in p.iter_proxies_with_domain() %}
{{ pd.domain.sni |ljust(30) }} sp_{{ p.id }}_{{ pd.domain.name|slugify | ljust(30) }}
{%- endfor %}
{% endfor %}
############## end of sni gateway ##############
